---
phase: 1.6-closing-verification-gaps
task: 1
total_tasks: 3
status: paused
last_updated: 2026-03-28T00:00:00Z
---

<current_state>
Phase 1.6 is paused to wait for external TCM/domain feedback on answer quality and prompt contract.
The prompt redesign task is intentionally parked so milestone progress can continue in parallel.
</current_state>

<completed_work>

- Full-text citation/context path is stabilized for newly generated citations.
- Same-origin backend proxy route is in place and frontend API calls target `/api/backend`.
- Trailing reference-section sanitizer is present in `web/components/MessageBubble.tsx` as a fallback guardrail.
- Parking artifacts created:
  - `.planning/todos/parked-001-tcm-expert-prompt-spec.md`
  - `.planning/phases/01-stabilization-bug-fixes/1.6-HANDOFF-APIS-PROMPT.md`
  - `.planning/HANDOFF.json`
</completed_work>

<remaining_work>

- Wait for expert response contract (structure, tone, safety policy, few-shot examples).
- Merge expert contract into `src/main.py` (`DEFAULT_SYSTEM_PROMPT`, `build_prompt_template()`).
- Validate with smoke tests across chat, citations, and mobile proxy behavior.
- Continue parallel tasks while blocked:
  - Phase 2 SymMap migration
  - Additional text-source ingestion
  - UI/UX polish tasks in roadmap
</remaining_work>

<decisions_made>

- Prompt redesign will be domain-led (expert-defined quality standard), not incremental patching.
- Rendering sanitizer remains as a safety net rather than primary control.
- Workstream should not stall: blocked prompt track is parked, parallel tasks continue.
</decisions_made>

<blockers>
- External domain feedback pending for final prompt/response contract.
</blockers>

<context>
This pause is intentional and aligned with GSD handoff practice: preserve state, mark blockers, and continue non-blocked tracks.
Use machine-readable `.planning/HANDOFF.json` plus this file for fast context restoration in a new session.
</context>

<next_action>
Start with `/gsd-resume-work` and select parallel execution focus (recommended: Phase 2 SymMap KG migration) until expert feedback arrives.
</next_action>
