---
phase: 1.5-ui-refinements
plan: 1.5
type: execute
wave: 1
depends_on: ["1-PLAN.md"]
autonomous: true
requirements: [UI-REF-01, UI-REF-02, UI-REF-03]
---

<objective>
Execute UI refinements to improve the citation viewing experience and layout consistency.
</objective>

<tasks>

<task type="auto">
  <name>Task 1.5.1: Update Sidebar Layout in page.tsx</name>
  <files>web/app/page.tsx, web/components/CitationPanel.tsx</files>
  <action>
    Modify `web/app/page.tsx` and `web/components/CitationPanel.tsx` so that the `CitationPanel` pushes the `main` chat area instead of overlaying it.
    - Remove `fixed` and `z-50` from `CitationPanel`.
    - Ensure `CitationPanel` has a fixed width and `shrink-0`.
    - Ensure `main` in `page.tsx` handles the transition smoothly.
  </action>
  <verify>Check that `CitationPanel` is a sibling of `main` and they both fit in the flex container without overlay.</verify>
  <done>CitationPanel pushes ChatArea instead of overlaying.</done>
</task>

<task type="auto">
  <name>Task 1.5.2: Reorganize Citation Header</name>
  <files>web/components/CitationPanel.tsx</files>
  <action>
    Move "Source Chapter" metadata from `TextCitationContent` and any equivalent in `GraphCitationContent` to the header section of `CitationPanel`.
    - Extract `sourceDisplay` logic to `CitationPanel`.
    - Render it just below the "Classical text source" / "Knowledge Graph Fact" heading in the header.
  </action>
  <verify>Metadata is visible in the panel header.</verify>
  <done>Source metadata is moved to the header.</done>
</task>

<task type="auto">
  <name>Task 1.5.3: Ensure Context Toggle functionality</name>
  <files>web/components/CitationPanel.tsx</files>
  <action>
    Verify and fix any issues with the "View Full Paragraph" toggle.
    - Ensure it correctly calls `fetchChunkContext`.
    - Ensure it displays the full paragraph text with highlights.
    - Handle loading and error states gracefully.
  </action>
  <verify>Toggling "View Full Paragraph" loads and displays expanded text.</verify>
  <done>Context toggle is fully functional.</done>
</task>

</tasks>

<success_criteria>
- Citation panel pushes chat area.
- Source metadata is in the header.
- Full context toggle works reliably.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization-bug-fixes/1.5-SUMMARY.md`
</output>
